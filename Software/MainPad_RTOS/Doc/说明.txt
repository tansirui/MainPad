2017.6.21  更正硬件错误，OLED使用的PE2为PA15，丝印标错。使用时要关闭JTAG。

2017.6.29  USB接口DP缺少上拉，若用到USB功能飞线连接，使用1K或1.5K电阻上拉。 无驱动情况下识别为位置设备。

2017.6.30  填坑虚拟串口，移植官方例程问题太多放弃。用原子代码。移植主要修改usb中断函数，官方放在stm32it.c中。
           原子将中断函数放到hw_config.c中，配置文件platform_config.h定义平台类型，修改官方为F103平台即可。
		   测试发现虚拟串口没有串口好用。

2017.7.1  填坑SPI_FLASH,修改官方例程。使用SPI3，和jtag复用，需禁止JTAG使能SWD。OLED也存在复用情况，所以初始化OLED后
          不需在SPI3中再设置，否则需配置。
          填坑摄像头，摄像头接口只兼容原子ov7670。其它带FIFO摄像头需要自己接线。初始化配置较多直接复制原子的，初始化
          配置后一定要开启片选！！！由于没预留tft接口，只能用串口打印数据，很容易就可分辨数据是否正常。
		  二值化后用OLED显示有待开发。

2017.7.3  NRF测试通过，使用原子例程，仅做初始化测试。稳定的通信需要自己后期修改。注意NRF和flash用的spi模式不一样，
          需要重新配置。预留Io输出测试全部通过，未发现错误。输入有待验证。

2017.10.1 ucosii移植完成，参考strongerHuang博客的教程，测试代码亦使用strongerHuang的。测试通过。系统时钟初始化保留了原作者
          的配置。也可以使用原子的delay.c初始化。相关宏定义已做简化。 

		  

